name: Reusable Workflow

on:
  workflow_call:
    inputs:
      name:
        description: enter name
        required: true
        type: string

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: greet
        run: echo "welcome ${{ inputs.name }}"
